## Overview

This file deliberately contains structural issues for `readme_lint` to catch.

It has no H1, jumps a heading level, has an image without alt text, and links
to a file that does not exist next to it.

#### Buried subsection

This heading is H4 directly under an H2 — a skipped level (no H3 in between).

![](logo.png)

See the [missing design doc](docs/does-not-exist.md) for details.
